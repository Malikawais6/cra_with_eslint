import styled from "styled-components";
import {Props} from "./Icon.type"
const SIZE = {
  xsmall:'8px',
  small:'16px',
  medium:'24px',
  large:'32px',
  xlarge:'40px',
}

// export const IconStyles = styled.span<Props>`
//   color: ${(props:Props) => props.color ? props.color : "black" };
//   font-family: ${(props:Props) => props.fontFamily};
//   font-size: ${(props:Props) => props.size? SIZE[props.size]:SIZE.small};
//   font-weight: ${(props:Props) => props.bold ? "bold" : "normal"};
//   font-style: ${(props:Props) => props.fontStyle ? props.fontStyle  : "normal"};
// `;
